/** 
* @param {number} a 
* @param {number} b 
* @returns {number}
 * */
function add (a, b) {
	return a + b;
}

/** 
 * @param {One} info
 * @returns {string}
 * */
function createNewInfo (info) {
	return info.firstName + info.secondName + " -- " + info.age.toString();

}
